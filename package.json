{
  "name": "wasm-workshop",
  "version": "0.1.0",
  "scripts": {
    "build": "cd lib && wasm-pack build --target web -- --features wee_alloc",
    "test": "cd lib && cargo test && wasm-pack test --headless",
    "live": "live-server --no-browser --port=8000"
  },
  "author": "m4dz <code@m4dz.net>",
  "license": "AGPL-3.0",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
